# Secrets — NEVER commit (holds Telegram bot token + Claude auth token)
.env

# Runtime output / state
logs/
state/
*.log

# Transient daily priority overlay (date-gated instructor picks)
priority-today.md

# Backtest research tool — its own venv + generated run output (regenerable
# from the committed scripts; scripts are the durable artifact, not the CSVs)
scripts/backtest/.venv/
scripts/backtest/run_out*/
scripts/backtest/*.pkl
scripts/backtest/__pycache__/
scripts/__pycache__/
research/__pycache__/
