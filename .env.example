# Copy to .env (chmod 600) and fill in. NEVER commit the real .env — it is gitignored.

# --- GUI session (so cron's headless env can reach notify-send) ---
DISPLAY=":0"
DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/1000/bus"
XDG_RUNTIME_DIR="/run/user/1000"

# --- Claude headless ---
CLAUDE_BIN="claude"
CLAUDE_MODEL="claude-opus-4-8"
CLAUDE_PROJECT_DIR="$HOME/Projects/aria-trading"   # dir where data/universe.csv + scripts/ live
# Auth: export NOTHING here. Run `claude login` once on this machine (ambient
# session, ~/.claude/.credentials.json) and cron inherits it. Confirmed
# 2026-08-24: exporting CLAUDE_CODE_OAUTH_TOKEN (or ANTHROPIC_API_KEY) makes
# the headless `claude --print` authenticate via that token instead of the
# ambient login — and a token/API-key session carries no claude.ai connector
# access, so IBKR (and any other connector) silently fails to attach on every
# script-invoked run. Direct-shell testing worked the whole time because it
# never exported these vars.

# --- Telegram delivery (bot sends the daily report to your chat) ---
export TELEGRAM_BOT_TOKEN="123456789:AA-replace-with-your-bot-token"
export TELEGRAM_CHAT_ID="000000000"

# --- Stale-feed guard (annotation-only by default) ---
HARD_SKIP_ON_UNCHANGED="false"
